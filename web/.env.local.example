# Server-side fetches (used in Server Components / route handlers)
API_URL=http://localhost:4000/api
# Client-side fetches (used in 'use client' components) — must be prefixed NEXT_PUBLIC_
NEXT_PUBLIC_API_URL=http://localhost:4000/api
